---
phase: 08-transcript-ingestion-fact-grounding-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/lib/utils/quote-matcher.ts
  - frontend/src/components/workspace/ResizableSplitView.tsx
  - frontend/src/components/workspace/TranscriptPane.tsx
  - frontend/src/components/workspace/EpistemicCard.tsx
  - frontend/src/components/workspace/FactCardsPane.tsx
  - frontend/src/components/workspace/ClarificationFooter.tsx
  - frontend/src/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - UI-03
decisions:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-16
must_haves:
  truths:
    - "User can view resizable dual-pane layout on desktop and tabbed toggle on mobile"
    - "Transcript reader displays full normalized transcript with soft emerald/amber glowing badge highlights on quote matches"
    - "Clicking any fact card smoothly scrolls and centers the verbatim quote in the transcript reader with an anchor pulse animation"
    - "Clicking an anchored quote in the transcript selects and scrolls to the corresponding epistemic fact card"
    - "Epistemic cards can be filtered by segmented pills (All, Confirmed Facts, Inferred Points, Contradictions, Unknown Gaps) and searched"
    - "Workspace displays sticky progression footer leading into Phase 9 gap review"
  artifacts:
    - frontend/src/lib/utils/quote-matcher.ts
    - frontend/src/components/workspace/ResizableSplitView.tsx
    - frontend/src/components/workspace/TranscriptPane.tsx
    - frontend/src/components/workspace/EpistemicCard.tsx
    - frontend/src/components/workspace/FactCardsPane.tsx
    - frontend/src/components/workspace/ClarificationFooter.tsx
    - frontend/src/app/projects/[id]/page.tsx
  key_links:
    - "frontend/src/components/workspace/TranscriptPane.tsx consumes quote-matcher.ts to segment text without mutating DOM"
    - "frontend/src/app/projects/[id]/page.tsx coordinates active fact ID selection bidirectionally between TranscriptPane and FactCardsPane"
---

<objective>
Build the grounded dual-pane discovery workspace in `frontend/src/app/projects/[id]/page.tsx` featuring the resizable split view, transcript reader with soft glowing pulse highlights, categorized epistemic fact cards with search filtering, bidirectional click-to-highlight / click-to-fact scroll orchestration, and the Phase 9 clarification progression footer (`UI-03`).

Purpose: Transforms raw discovery transcripts into a verifiable, interactive epistemic partner where every fact is visibly linked to verbatim evidence.
Output: ResizableSplitView, TranscriptPane, EpistemicCard, FactCardsPane, ClarificationFooter, quote-matcher utility, and the integrated workspace page.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-transcript-ingestion-fact-grounding-ui/08-CONTEXT.md
@.planning/phases/08-transcript-ingestion-fact-grounding-ui/08-UI-SPEC.md
@.planning/phases/08-transcript-ingestion-fact-grounding-ui/08-RESEARCH.md
@.planning/phases/08-transcript-ingestion-fact-grounding-ui/08-01-PLAN.md
</context>

<tasks>

<task type="auto" name="build-quote-matcher-and-split-view">
<action>
1. In `frontend/src/lib/utils/quote-matcher.ts`:
   - Implement `findQuoteMatches(transcriptText: string, quotes: Array<{ id: string; quote: string }>)`.
   - Normalize spaces and linebreaks (`\s+` -> ` `) during search comparison to ensure exact matching across formatting shifts.
   - Return structured non-overlapping text segments: `{ text: string, isQuote: boolean, factId?: string, quoteId?: string }[]`.
2. In `frontend/src/components/workspace/ResizableSplitView.tsx`:
   - Desktop view: Render Left Pane, Resizer Drag Handle (with hover and active border accents), and Right Pane.
   - Mouse event drag listener tracking horizontal percentage, clamped between 30% and 70% (default 45% / 55%).
   - Mobile/tablet view: Render segmented tab toggle (`Transcript` \| `Facts ({count})`) with active-fact indicator badge, showing only the selected pane.
</action>
<verify>
<automated>npx tsc -p frontend/tsconfig.json --noEmit</automated>
</verify>
</task>

<task type="auto" name="build-transcript-pane-and-fact-cards">
<action>
1. In `frontend/src/components/workspace/TranscriptPane.tsx`:
   - Render segmented transcript spans from `quote-matcher.ts`.
   - Unmatched text renders as high-contrast body text (`text-zinc-300`).
   - Matched quotes render with styling `bg-emerald-500/20 text-emerald-200 border-l-2 border-emerald-400 rounded-sm px-1 py-0.5 cursor-pointer hover:bg-emerald-500/30 transition-colors`.
   - Active quote renders with soft glow ring and pulse animation.
   - Clicking an anchored quote triggers `onSelectFact(factId)`.
   - Expose ref or method to smooth-scroll and vertically center any quote element (`#quote-${factId}`) with 1.5s anchor flash animation when activated from fact cards.
2. In `frontend/src/components/workspace/EpistemicCard.tsx`:
   - Support types: `confirmed`, `inferred`, `contradiction`, `unknown`.
   - Render: Bold statement, speaker tag pill, category badge, verbatim quote block with `Locate in Transcript` button (with quote/crosshair icon), and inferred reasoning note for inferred items.
   - Active card state: `border-emerald-500/60 ring-1 ring-emerald-500/40 bg-zinc-900/90`.
   - Clicking the card or its quote preview invokes `onSelectQuote(factId, quote)`.
3. In `frontend/src/components/workspace/FactCardsPane.tsx`:
   - Top bar: Segmented filter pill tabs: `All ({total})`, `Confirmed ({confirmed})`, `Inferred ({inferred})`, `Contradictions ({contradictions})`, `Unknowns ({unknowns})`.
   - Search input filtering card statements and quotes by keyword.
   - Scrollable container rendering the filtered `EpistemicCard` list.
</action>
<verify>
<automated>npx tsc -p frontend/tsconfig.json --noEmit</automated>
</verify>
</task>

<task type="auto" name="integrate-project-workspace-page">
<action>
1. In `frontend/src/components/workspace/ClarificationFooter.tsx`:
   - Sticky bottom footer bar showing:
     - Extraction summary (e.g. `{confirmed} confirmed facts grounded • {unknowns + contradictions} items need clarification`).
     - Primary button: `Proceed to Clarifications ({N} items) →` (disabled or placeholder toast for Phase 9).
2. In `frontend/src/app/projects/[id]/page.tsx`:
   - Fetch project using `useProject(id)`.
   - Fetch transcripts using `useTranscripts(id)`.
   - Fetch extraction state using `useAnalysis(id)`.
   - If no transcript exists: Render `IngestionHero` with `Start Epistemic Analysis` CTA.
   - If transcript exists:
     - Render sticky workspace subheader with breadcrumb, project title, status badge, and `Replace Transcript` action.
     - If status is `analyzing`: Display animated progress banner with skeleton loading.
     - If status is `awaiting_clarification` or beyond: Render `ResizableSplitView` with `TranscriptPane` on left and `FactCardsPane` on right, bidirectionally wired with `activeFactId` state.
     - Render `ClarificationFooter` at the bottom.
3. Validate complete TypeScript strict compilation and Next.js production build.
</action>
<verify>
<automated>npm --prefix frontend run build</automated>
</verify>
</task>

</tasks>
