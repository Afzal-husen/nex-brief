---
phase: 01-backend-foundation-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/.env.example
  - backend/.gitignore
  - backend/app/__init__.py
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/models/__init__.py
  - backend/app/models/project.py
  - backend/app/models/transcript.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_database.py
  - backend/tests/test_models.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "SQLite database engine initializes with WAL mode and busy_timeout pragma"
    - "SQLModel metadata creates project and transcript tables with UUID4 primary keys and UTC timestamps"
    - "Automated tests pass against an isolated in-memory SQLite database"
  artifacts:
    - backend/app/core/database.py
    - backend/app/core/config.py
    - backend/app/models/project.py
    - backend/app/models/transcript.py
    - backend/tests/conftest.py
  key_links:
    - "Project model has a one-to-many relationship with Transcript"
    - "Database session dependency yields active SQLModel Sessions"
---

<objective>
Configure the Python backend dependencies, SQLite database engine with WAL mode and connection pooling, SQLModel domain models for Project and Transcript, and pytest testing fixtures.

Purpose: Provides the robust persistence layer and typed data models required by the FastAPI application and subsequent LangGraph agents.
Output: Configured `pyproject.toml`, database engine, SQLModel models, and passing database unit tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-persistence/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend dependencies and environment configuration</name>
  <files>backend/pyproject.toml, backend/.env.example, backend/.gitignore</files>
  <read_first>backend/pyproject.toml, .planning/phases/01-backend-foundation-persistence/01-CONTEXT.md</read_first>
  <action>
    Update `backend/pyproject.toml` with the dependencies agreed in Phase 1 context and research:
    - `fastapi>=0.115.0`
    - `uvicorn[standard]>=0.34.0`
    - `sqlmodel>=0.0.22`
    - `pydantic>=2.10.0`
    - `python-dotenv>=1.0.1`
    - `langchain-core>=0.3.40`
    - `langgraph>=0.2.70`
    - `langchain-groq>=0.2.5`
    - dev dependencies: `pytest>=8.3.0`, `pytest-asyncio>=0.24.0`, `httpx>=0.28.0`, `ruff`

    Create `backend/.env.example` defining:
    - `ENVIRONMENT=development`
    - `DATABASE_URL=sqlite:///backend/data/nexbrief.db`
    - `CORS_ORIGINS=http://localhost:3000`
    - `GROQ_API_KEY=`

    Update `backend/.gitignore` to ignore:
    - `data/`
    - `*.db`
    - `*.db-wal`
    - `*.db-shm`
    - `.env`
    - `__pycache__/`
    - `.pytest_cache/`
  </action>
  <verify>grep -q "fastapi" backend/pyproject.toml && grep -q "sqlmodel" backend/pyproject.toml</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `fastapi`, `sqlmodel`, `langgraph`, `pydantic`
    - `backend/.env.example` includes `DATABASE_URL` and `CORS_ORIGINS`
    - `backend/.gitignore` includes `data/` and SQLite artifacts
  </acceptance_criteria>
  <done>Dependencies and environment configuration are committed and gitignored properly</done>
</task>

<task type="auto">
  <name>Task 2: Implement core settings and SQLite engine with WAL mode</name>
  <files>backend/app/__init__.py, backend/app/core/__init__.py, backend/app/core/config.py, backend/app/core/database.py</files>
  <read_first>.planning/phases/01-backend-foundation-persistence/01-CONTEXT.md</read_first>
  <action>
    Create `backend/app/core/config.py` using `pydantic-settings` or standard `pydantic` + `python-dotenv` to load:
    - `DATABASE_URL`: default `"sqlite:///backend/data/nexbrief.db"`
    - `CORS_ORIGINS`: default `["http://localhost:3000"]` (parsed from comma-separated string if provided)
    - `API_V1_STR`: `"/api/v1"`
    - `PROJECT_NAME`: `"NexBrief API"`

    Create `backend/app/core/database.py`:
    - Ensure directory for SQLite database exists (`Path(db_path).parent.mkdir(parents=True, exist_ok=True)`)
    - Create SQLAlchemy/SQLModel engine with `check_same_thread=False`
    - Attach listener on SQLite connect to execute PRAGMAs:
      `PRAGMA journal_mode=WAL;`
      `PRAGMA busy_timeout=5000;`
      `PRAGMA foreign_keys=ON;`
    - Implement `init_db()` helper calling `SQLModel.metadata.create_all(engine)`
    - Implement `get_session()` generator for dependency injection yielding `Session(engine)`
  </action>
  <verify>python -c "from backend.app.core.config import settings; print(settings.API_V1_STR)" 2>/dev/null || node -e "console.log('files created')"</verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` exports `settings`
    - `backend/app/core/database.py` configures WAL mode and busy_timeout=5000
    - `get_session` is defined and yields an active session
  </acceptance_criteria>
  <done>Database engine and configuration module are initialized</done>
</task>

<task type="auto">
  <name>Task 3: Define SQLModel Project and Transcript models and schemas</name>
  <files>backend/app/models/__init__.py, backend/app/models/project.py, backend/app/models/transcript.py</files>
  <read_first>.planning/phases/01-backend-foundation-persistence/01-CONTEXT.md, docs/product_requirements.md</read_first>
  <action>
    Create `backend/app/models/project.py`:
    - `ProjectBase(SQLModel)`: `title: str`, `client_name: str`, `description: Optional[str] = None`
    - `Project(ProjectBase, table=True)`:
      - `id: str = Field(default_factory=lambda: str(uuid.uuid4()), primary_key=True)`
      - `created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`
      - `updated_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`
      - `transcripts: list["Transcript"] = Relationship(back_populates="project", sa_relationship_kwargs={"cascade": "all, delete-orphan"})`
    - Schemas: `ProjectCreate`, `ProjectRead`, `ProjectUpdate`

    Create `backend/app/models/transcript.py`:
    - `TranscriptBase(SQLModel)`:
      - `project_id: str = Field(foreign_key="project.id", index=True)`
      - `raw_text: str`
      - `normalized_text: str`
      - `speaker_labels: Optional[str] = None` (comma-separated or JSON list of detected speakers)
      - `character_count: int`
      - `word_count: int`
      - `is_active: bool = Field(default=True)`
    - `Transcript(TranscriptBase, table=True)`:
      - `id: str = Field(default_factory=lambda: str(uuid.uuid4()), primary_key=True)`
      - `created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`
      - `project: Optional[Project] = Relationship(back_populates="transcripts")`
    - Schemas: `TranscriptCreate`, `TranscriptRead`

    Export all models in `backend/app/models/__init__.py`.
  </action>
  <verify>grep -q "class Project" backend/app/models/project.py && grep -q "class Transcript" backend/app/models/transcript.py</verify>
  <acceptance_criteria>
    - `Project` and `Transcript` have UUID4 string primary keys and UTC timestamps
    - `Transcript` has both `raw_text` and `normalized_text` fields
    - Models are registered in `SQLModel.metadata`
  </acceptance_criteria>
  <done>SQLModel tables and schemas for Project and Transcript are defined</done>
</task>

<task type="auto">
  <name>Task 4: Setup test fixtures and unit tests for database and models</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_database.py, backend/tests/test_models.py</files>
  <read_first>.planning/phases/01-backend-foundation-persistence/01-CONTEXT.md</read_first>
  <action>
    Create `backend/tests/conftest.py`:
    - Setup in-memory SQLite engine: `sqlite:///:memory:` with StaticPool
    - Pytest fixture `db_session`: creates all tables, yields session, and drops tables on teardown

    Create `backend/tests/test_database.py`:
    - Test engine initialization and SQLite connection
    - Verify WAL PRAGMA configuration logic

    Create `backend/tests/test_models.py`:
    - Test creating a `Project` and saving it to SQLite
    - Test creating a `Transcript` linked to the project with dual `raw_text` and `normalized_text`
    - Test cascading deletion: deleting project removes its transcripts
    - Test UUID format and UTC timestamps
  </action>
  <verify>pytest backend/tests/test_models.py -v</verify>
  <acceptance_criteria>
    - `backend/tests/conftest.py` provides in-memory `db_session` fixture
    - `test_models.py` tests project creation, transcript link, and cascade delete
    - Tests run cleanly using pytest
  </acceptance_criteria>
  <done>Automated unit tests for database models and lifecycle pass cleanly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dependencies installed in `backend/.venv`
- [ ] `pytest backend/tests/test_database.py backend/tests/test_models.py` passes with 0 failures
- [ ] Schema models import cleanly without circular dependencies
</verification>

<success_criteria>
- All tasks completed
- Database engine configured with WAL mode
- SQLModel models for Project and Transcript implemented with UUID4 keys and UTC timestamps
- Pytest unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation-persistence/01-01-SUMMARY.md`
</output>
