---
phase: 04-brief-synthesis-critique-durable-checkpointing
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/app/graph/builder.py
  - backend/app/services/extraction.py
  - backend/tests/test_state_checkpointing.py
  - backend/tests/test_extraction_graph.py
autonomous: true
requirements:
  - DATA-04
decisions:
  - D-13
  - D-14
  - D-15
  - D-16
must_haves:
  truths:
    - "LangGraph workflow is extended to a 6-node sequential pipeline: extract_knowledge -> verify_grounding -> detect_contradictions -> generate_clarifications -> synthesize_brief -> critique_brief -> END"
    - "SqliteSaver persists state checkpoints in SQLite tables across process restarts"
    - "Thread IDs isolate states using project and transcript identifiers"
    - "Hermetic integration test verifies full pipeline execution and state resumption after simulated process termination"
  artifacts:
    - backend/app/graph/builder.py
    - backend/app/services/extraction.py
    - backend/tests/test_state_checkpointing.py
  key_links:
    - "backend/app/graph/builder.py assembles all 6 nodes with checkpointer support"
    - "backend/app/services/extraction.py runs the full pipeline with SqliteSaver context"
---

<objective>
Wire the synthesis and critique nodes into an extended 6-node LangGraph pipeline with durable SQLite checkpointing, and implement hermetic integration tests verifying state resumption across process restarts.

Purpose: Delivers requirement DATA-04 to guarantee all graph execution state is persisted durably and recoverable.
Output: Extended 6-node graph in `backend/app/graph/builder.py`, updated pipeline service in `backend/app/services/extraction.py`, and checkpoint resumption test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-brief-synthesis-critique-durable-checkpointing/04-CONTEXT.md
@.planning/phases/04-brief-synthesis-critique-durable-checkpointing/04-RESEARCH.md
@.planning/phases/04-brief-synthesis-critique-durable-checkpointing/04-01-PLAN.md
@backend/app/graph/builder.py
@backend/app/services/extraction.py
</context>

<tasks>

<task id="04-02-01" wave="2">
<title>Wire 6-Node Pipeline and Update Extraction Service</title>
<description>
Update `backend/app/graph/builder.py`:
- Import `synthesize_brief_node` and `critique_brief_node`.
- Extend graph workflow:
  `workflow.add_node("synthesize_brief", synthesize_brief_node)`
  `workflow.add_node("critique_brief", critique_brief_node)`
  `workflow.add_edge("generate_clarifications", "synthesize_brief")`
  `workflow.add_edge("synthesize_brief", "critique_brief")`
  `workflow.add_edge("critique_brief", END)`
Update `backend/app/services/extraction.py`:
- Include `user_clarifications` in `initial_state`.
- Format thread ID: `thread_id = f"project:{project_id}:transcript:{transcript_id}"` (or `transcript_id`).
- Return `draft_brief` and `critique_report` in `ExtractionResult` or service response.
Update `backend/tests/test_extraction_graph.py` to verify the 6-node topology.
</description>
<read_first>
- backend/app/graph/builder.py
- backend/app/services/extraction.py
- backend/app/graph/state.py
</read_first>
<files>
- backend/app/graph/builder.py
- backend/app/services/extraction.py
- backend/tests/test_extraction_graph.py
</files>
<automated>
backend/.venv/Scripts/python.exe -m pytest backend/tests/test_extraction_graph.py
</automated>
</task>

<task id="04-02-02" wave="2">
<title>Implement SQLite Checkpoint State Recovery Integration Tests</title>
<description>
Create `backend/tests/test_state_checkpointing.py`:
- Test initialization of `SqliteSaver` with temporary SQLite database.
- Execute graph partially (e.g. through extraction & clarifications) with a unique thread ID.
- Simulate process termination by closing the checkpointer connection and tearing down the graph instance.
- Create a fresh graph instance pointing to the same SQLite database file.
- Inspect state at the thread ID via `graph.get_state(config)` and verify state restoration of confirmed facts, inferences, contradictions, and questions.
- Resume execution with injected `user_clarifications` to run `synthesize_brief` and `critique_brief` to completion.
- Verify final state in SQLite contains the synthesized 11-section `draft_brief` and `critique_report`.
</description>
<read_first>
- backend/app/services/extraction.py
- backend/app/graph/builder.py
</read_first>
<files>
- backend/tests/test_state_checkpointing.py
</files>
<automated>
backend/.venv/Scripts/python.exe -m pytest backend/tests/test_state_checkpointing.py
</automated>
</task>

</tasks>
