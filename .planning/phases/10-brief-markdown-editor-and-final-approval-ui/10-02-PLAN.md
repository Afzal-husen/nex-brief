---
phase: 10-brief-markdown-editor-and-final-approval-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/lib/utils/export-brief.ts
  - frontend/src/components/brief/BriefMarkdownEditor.tsx
  - frontend/src/components/brief/CritiqueInlineAlert.tsx
  - frontend/src/components/brief/BriefSectionCard.tsx
  - frontend/src/components/brief/ApprovalModal.tsx
  - frontend/src/app/projects/[id]/brief/page.tsx
autonomous: true
requirements:
  - UI-05
  - BRIEF-04
  - EVAL-01
decisions:
  - D-03
  - D-04
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
must_haves:
  truths:
    - "User can edit any of the 11 brief sections using an interactive Markdown editor with live preview"
    - "Clicking 'Apply Suggested Fix' on a critique issue incorporates the AI suggestion into the section content"
    - "Section cards indicate when content has been modified by the editor"
    - "User can reset any edited section back to the original AI draft"
    - "Clicking 'Review & Approve' opens ApprovalModal showing editorial modification summary and diff highlights"
    - "Confirming approval invokes POST /api/v1/projects/{id}/approve, updates status to approved, and records correction diffs"
    - "User can copy the full brief Markdown or download a sanitized .md file with 1-click export actions"
  artifacts:
    - frontend/src/lib/utils/export-brief.ts
    - frontend/src/components/brief/BriefMarkdownEditor.tsx
    - frontend/src/components/brief/ApprovalModal.tsx
  key_links:
    - "frontend/src/components/brief/ApprovalModal.tsx invokes apiClient.workflow.approve"
    - "frontend/src/components/brief/BriefSectionCard.tsx embeds BriefMarkdownEditor when editing"
---

<objective>
Build the interactive Markdown section editor, live preview, 1-click critique fix application, diff review modal, and final approval sign-off workflow (`UI-05`, `BRIEF-04`, `EVAL-01`).
Purpose: Give operators complete editorial authority to refine AI-drafted sections, resolve critique findings, review change diffs, and finalize the brief for client delivery while logging corrections for evaluation.
Output: Section markdown editor, critique applicator, export utility, approval modal, and full approval loop.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-brief-markdown-editor-and-final-approval-ui/10-CONTEXT.md
@.planning/phases/10-brief-markdown-editor-and-final-approval-ui/10-UI-SPEC.md
@.planning/phases/10-brief-markdown-editor-and-final-approval-ui/10-01-PLAN.md
</context>

<tasks>

<task type="auto" name="markdown-editor-and-critique-integration">
<action>
1. Create `frontend/src/components/brief/BriefMarkdownEditor.tsx`:
   - Interactive Markdown textarea with syntax formatting shortcuts (bold, italic, list, heading).
   - Live preview tab or split-pane view displaying rendered output in real time.
   - Word and character count meters.
   - "Save Section" action, "Cancel" action, and "Reset to AI Draft" action.
2. In `frontend/src/components/brief/CritiqueInlineAlert.tsx`:
   - Add `onApplyFix?: (fixText: string) => void` prop.
   - Render a 1-click "Apply Suggested Fix" button that passes the AI's suggested fix to the active section editor.
3. In `frontend/src/components/brief/BriefSectionCard.tsx`:
   - Support toggling between view mode and editing mode (`BriefMarkdownEditor`).
   - Track local edited content vs original draft content.
   - Display "Modified by editor" badge when section differs from draft.
   - Expose handler to receive suggested fix text and update active section content.
</action>
<verify>
<automated>npm --prefix frontend run build</automated>
</verify>
</task>

<task type="auto" name="approval-modal-and-export-system">
<action>
1. Create `frontend/src/lib/utils/export-brief.ts`:
   - `copyBriefToClipboard(markdown: string): Promise<boolean>`.
   - `downloadBriefMarkdown(projectName: string, markdown: string): void`.
   - Helper to stitch the 11 section titles and contents into unified GitHub Flavored Markdown document.
2. Create `frontend/src/components/brief/ApprovalModal.tsx`:
   - Backdrop modal displaying:
     - Editorial modification summary (e.g. "X of 11 sections modified by human editor").
     - Diff highlights or preview of modified sections vs draft sections.
     - "Confirm Final Approval" primary button with loading spinner calling `apiClient.workflow.approve`.
     - Instant export actions: "Copy Full Brief (Markdown)", "Download .md File".
     - Close / cancel button.
3. In `frontend/src/app/projects/[id]/brief/page.tsx`:
   - Wire state for all 11 sections (draft + edited overrides).
   - Wire "Review & Approve" trigger in header and bottom action bar.
   - Handle approval response, mutate project and brief state to `approved`.
   - Display "Approved & Grounded" stamp when project is in approved status.
</action>
<verify>
<automated>npm --prefix frontend run build</automated>
</verify>
</task>

</tasks>

<must_haves>
- [x] Inline Markdown editing with live preview for each section
- [x] 1-Click "Apply Suggested Fix" from critique alerts
- [x] Reset to original AI draft functionality
- [x] ApprovalModal displaying editorial changes and diff summary
- [x] POST /projects/{id}/approve call on confirmation updating project status to approved
- [x] 1-Click clipboard copy and .md file download
- [x] Clean Next.js build with zero TypeScript or linting errors
</must_haves>
