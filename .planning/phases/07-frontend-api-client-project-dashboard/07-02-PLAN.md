---
phase: 07-frontend-api-client-project-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/dashboard/ProjectCard.tsx
  - frontend/src/components/dashboard/ProjectMetrics.tsx
  - frontend/src/components/dashboard/CreateProjectModal.tsx
  - frontend/src/components/dashboard/DeleteProjectModal.tsx
  - frontend/src/components/dashboard/EmptyState.tsx
  - frontend/src/components/dashboard/ProjectGrid.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/projects/[id]/page.tsx
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - UI-01
decisions:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
must_haves:
  truths:
    - "User can view responsive card grid of all projects on the main dashboard"
    - "Each card displays title, description snippet, status badge with animated pulse for active processing, and created date"
    - "Clicking a project card navigates to /projects/[id]"
    - "User can open Create Project modal, submit title & description, and auto-navigate to the new project"
    - "User can open Delete Project confirmation dialog and permanently delete a project with immediate list mutation"
    - "Search input filters project cards in real-time by title or description"
    - "Empty state is rendered when no projects exist with an inviting CTA to create a project"
    - "Top metric summary pills display counts for Total, In Progress, Needs Clarification, and Approved"
  artifacts:
    - frontend/src/components/dashboard/ProjectCard.tsx
    - frontend/src/components/dashboard/ProjectMetrics.tsx
    - frontend/src/components/dashboard/CreateProjectModal.tsx
    - frontend/src/components/dashboard/DeleteProjectModal.tsx
    - frontend/src/components/dashboard/EmptyState.tsx
    - frontend/src/components/dashboard/ProjectGrid.tsx
    - frontend/src/app/page.tsx
    - frontend/src/app/projects/[id]/page.tsx
  key_links:
    - "frontend/src/components/dashboard/ProjectGrid.tsx uses useProjects from frontend/src/lib/api/hooks.ts"
    - "frontend/src/app/layout.tsx wraps the app with ToastProvider"
---

<objective>
Build the Project Dashboard, responsive Bento card grid, metric summary pills, search filter, Create Project modal, Delete Project confirmation dialog, and Project pipeline layout scaffold (`UI-01`).

Purpose: Completes Phase 7 requirements, providing a polished, high-density project management interface ready for transcript ingestion in Phase 8.
Output: Complete dashboard views, modals, routing, and project detail workspace scaffold.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-api-client-project-dashboard/07-CONTEXT.md
@.planning/phases/07-frontend-api-client-project-dashboard/07-UI-SPEC.md
@.planning/phases/07-frontend-api-client-project-dashboard/07-RESEARCH.md
@.planning/phases/07-frontend-api-client-project-dashboard/07-01-PLAN.md
</context>

<tasks>

<task id="07-02-01" wave="1">
<name>Build Dashboard metric summary and project card components</name>
<description>
Create `frontend/src/components/dashboard/ProjectMetrics.tsx`:
- Accepts `projects: Project[]`.
- Computes counts: `Total`, `In Progress` (`analyzing` + `synthesizing`), `Needs Clarification` (`awaiting_clarification`), and `Approved`.
- Renders horizontal row of compact metric pills with Lucide icons.

Create `frontend/src/components/dashboard/ProjectCard.tsx`:
- Accepts `project: Project` and `onDelete: (project: Project) => void`.
- Whole card links or navigates to `/projects/${project.id}` with hover highlight.
- Header: Title with `line-clamp-1` font-semibold, and `Badge` showing current status.
- Body: Description with `line-clamp-2` in muted text, or placeholder if none provided.
- Footer: Formatted date (e.g. "Created 2 hours ago" or localized date string), and discrete delete button (`Trash2` icon) with `e.stopPropagation()` to prevent navigation when clicking delete.
</description>
<verify>
<automated>npx --prefix frontend tsc --noEmit</automated>
</verify>
</task>

<task id="07-02-02" wave="1">
<name>Build Create Project and Delete Confirmation modals</name>
<description>
Create `frontend/src/components/dashboard/CreateProjectModal.tsx`:
- Controlled modal using `Modal.tsx`.
- Form inputs: Project Title (required, autofocus, input validation) and Description (optional textarea).
- On submit: calls `apiClient.projects.create()`, handles loading state on button, revalidates SWR cache via `mutate()`, dispatches success toast, and calls `router.push('/projects/${created.id}')`.
- Captures API errors and displays both inline message and error toast.

Create `frontend/src/components/dashboard/DeleteProjectModal.tsx`:
- Controlled modal asking for explicit confirmation to delete project.
- Displays project title in bold, warning copy ("All transcripts, extractions, and briefs will be permanently deleted").
- On confirm: calls `apiClient.projects.delete(project.id)`, optimistically updates SWR cache, dispatches success toast, and closes modal.
</description>
<verify>
<automated>npx --prefix frontend tsc --noEmit</automated>
</verify>
</task>

<task id="07-02-03" wave="1">
<name>Build ProjectGrid, EmptyState, Dashboard page, and Project scaffold</name>
<description>
Create `frontend/src/components/dashboard/EmptyState.tsx`:
- Renders empty state container with folder/sparkles icon, "No discovery projects yet" heading, "Create your first project to ingest call transcripts and extract grounded briefs." description, and "Create Project" primary button.

Create `frontend/src/components/dashboard/ProjectGrid.tsx`:
- Fetches data using `useProjects()`.
- Search input with search icon, filtering projects in real-time by title and description.
- Status filter dropdown or pills (All, In Progress, Ready, Approved).
- Loading state: renders 3-6 `Skeleton` cards.
- Error state: displays error alert with retry button.
- Empty state: renders `EmptyState` when project list is empty or when search returns 0 results.
- Populated state: renders responsive grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`) of `ProjectCard`s.

Update `frontend/src/app/layout.tsx`:
- Wrap children with `ToastProvider` to enable global notifications.

Update `frontend/src/app/page.tsx`:
- Header with NexBrief branding, product tagline ("Client Discovery to Verifiable Brief"), and "+ New Project" button opening `CreateProjectModal`.
- `ProjectMetrics` bar.
- `ProjectGrid`.

Create `frontend/src/app/projects/[id]/page.tsx`:
- Workspace page header with "← Back to Dashboard" navigation link, project title, status `Badge`, and timestamp.
- Placeholder container for Phase 8 transcript ingestion workspace.
</description>
<verify>
<automated>npm --prefix frontend run build</automated>
</verify>
</task>

</tasks>

<must_haves>
All interactive components work seamlessly without console errors. Page builds cleanly with Next.js 16 compiler.
</must_haves>
